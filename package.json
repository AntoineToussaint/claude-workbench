{
  "name": "claude-workbench",
  "version": "0.1.0",
  "description": "Parallel Claude Code workbench — color-coded worktrees, tmux, one-click terminal/editor launch",
  "main": "electron/main.js",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/AntoineToussaint/claude-workbench.git"
  },
  "keywords": ["claude", "claude-code", "workbench", "tmux", "parallel", "worktree"],
  "bin": {
    "claude-workbench": "./bin/claude-workbench.js"
  },
  "files": [
    "bin/",
    "lib/",
    "routes/",
    "dist/",
    "electron/",
    "db.js",
    "server.js",
    "index.html"
  ],
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node server.js",
    "start:reset": "node server.js --reset",
    "dev:all": "node server.js & pnpm vite & wait",
    "test": "vitest run",
    "prepublishOnly": "npm run build",
    "postinstall": "npx @electron/rebuild --only better-sqlite3 2>/dev/null || true",
    "electron:dev": "npm run build && electron .",
    "electron:build": "npm run build && electron-builder build --mac"
  },
  "dependencies": {
    "@dnd-kit/core": "^6.3.1",
    "@dnd-kit/sortable": "^10.0.0",
    "@dnd-kit/utilities": "^3.2.2",
    "better-sqlite3": "^12.6.2",
    "express": "^4.21.2",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.4.1",
    "vite": "^6.4.1",
    "vitest": "^4.1.0",
    "electron": "^35.0.0",
    "electron-builder": "^26.0.0",
    "@electron/notarize": "^3.0.0"
  }
}
